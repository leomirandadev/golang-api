version: "2"

services:
  app:
    image: golang:1.6.1-alpine
    volumes:
      - ../../:/go/src/vcfConverter
    working_dir: /go/src/vcfConverter
    command: go run main.go
    ports:
      - "80:80"